# Store the real environment file outside the repository with service-account-only access.
APP_PROFILE=legacy
LEGACY_SQLITE_PATH=/absolute/path/to/approved-snapshot.sqlite
APP_ORIGIN=http://127.0.0.1:PORT
APP_PORT=PORT
# Set only when persistence has been authorized.
# SCIENTIFIC_DATABASE_URL=
# OPERATIONAL_DATABASE_URL=

# Scientific database boundary

The future scientific plane serves immutable snapshot materializations only. It must never contain user identity, workspace, or operational workflow state. No driver or connection is implemented here.

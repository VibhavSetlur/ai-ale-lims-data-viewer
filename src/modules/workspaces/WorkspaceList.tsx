'use client';
/* eslint-disable react-hooks/set-state-in-effect -- localStorage hydration initializes client-only catalog state. */
import Link from "next/link";
import { useEffect, useState } from "react";
import { createWorkspace, deleteWorkspace, duplicateWorkspace, loadStore, renameWorkspace, saveStore } from "./local-repository";
import type { LocalWorkspaceStoreV1 } from "./contracts";
export function WorkspaceList() { const [store, setStore] = useState<LocalWorkspaceStoreV1>(); const [error, setError] = useState(""); const load = () => { const result = loadStore(window.localStorage); if (result.ok) setStore(result.value); else { setStore(result.value); setError(result.message); } }; useEffect(() => { load(); const onStorage = (event: StorageEvent) => { if (event.key === "viewer2.workspaces.v1") load(); }; window.addEventListener("storage", onStorage); return () => window.removeEventListener("storage", onStorage); }, []); const persist = (next: LocalWorkspaceStoreV1) => { const saved = saveStore(window.localStorage, next); if (saved.ok) setStore(saved.value); else setError(saved.message); }; const create = () => { if (!store) return; const next = createWorkspace(store); if (!next.ok) return setError(next.message); persist(next.value); }; const rename = (id: string, current: string) => { if (!store) return; const name = prompt("Workspace name", current); if (name === null) return; const next = renameWorkspace(store, id, name); if (!next.ok) return setError(next.message); persist(next.value); }; const duplicate = (id: string) => { if (!store) return; const next = duplicateWorkspace(store, id); if (!next.ok) return setError(next.message); persist(next.value); }; const remove = (id: string) => { if (!store || !confirm("Delete this browser-local workspace?")) return; persist(deleteWorkspace(store, id)); }; return <section><header className="page-header"><p className="eyebrow">BROWSER-LOCAL</p><h1>Saved / Local workspaces</h1><p className="lede">Browser-local only. Never writes to LIMS.</p></header>{error && <p role="alert">{error}</p>}<button onClick={create}>Create workspace</button><ul>{store?.workspaces.map(workspace => <li key={workspace.id}><Link href={`/plates/${workspace.id}`}>{workspace.name}</Link> <button onClick={() => rename(workspace.id, workspace.name)}>Rename</button> <button onClick={() => duplicate(workspace.id)}>Duplicate</button> <button onClick={() => remove(workspace.id)}>Delete</button></li>)}</ul>{store && !store.workspaces.length && <p>No local workspaces yet.</p>}</section>; }

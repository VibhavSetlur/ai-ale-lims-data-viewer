SQLITE_PATH=data/lims_mirror.backup.db
# MYSQL_URL=mysql://user:pass@host:3306/lims

# Mutation Explorer dataset. Auto-detected by file extension.
# Supported formats: .json (wide or long), .csv, .tsv / .tab
# Default if unset: data/mutations.json
# MUTATIONS_PATH=data/mutations.json
# MUTATIONS_PATH=data/natascha_mutations.csv
# MUTATIONS_PATH=/abs/path/to/spreadsheet.tsv
